import styled from "styled-components";

const Card = styled.div`
  background: #fff;
  box-shadow: var(--shadow-2);
`;

export const CardBorrower = styled.div`
  background: #fff;
  box-shadow: 0px 12px 22px rgba(0, 0, 0, 0.12);
`;

export default Card;
